interface Props { projectId: string }

export function BacklogView({ projectId: _projectId }: Props) {
  return (
    <div className="flex items-center justify-center h-full text-slate-400 text-sm">
      Backlog view — coming soon (Task 9)
    </div>
  )
}
