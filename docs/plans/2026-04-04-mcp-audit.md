# MCP Tool Audit — 2026-04-04

**Project:** BOARD (Agent Board)
**Board:** https://agent-board-production-7859.up.railway.app
**Scope:** All 25+ MCP tools exercised against the live Railway deployment.

---

## Summary

| Category | Count |
|---|---|
| Tools tested | 25 |
| ✅ Working | 21 |
| 🐛 Bugs found | 4 |
| 💡 Missing features | 5 |

---

## ✅ Working

All of the following tools were confirmed working against the deployed server:

**Read operations**
- `list_projects` — returns all projects for the API key user (admin)
- `list_epics` — by project_id
- `list_features` — by epic_id or project_id
- `get_epic` — with nested features and story count rollups
- `get_feature` — with stories list and parent epic
- `get_story` — with full event history and links
- `get_board` — all stories for a project grouped (flat list)
- `get_project_overview` — full hierarchy: epics → features → story counts + recent activity
- `list_agents` — full agent roster with slugs, skills, colors

**Write / CRUD**
- `create_project` — with key, name, description, workflow_id
- `create_epic` — with project_id, title, description, version
- `update_epic` — title, description, version, status, start_date, end_date
- `create_feature` — with epic_id, title, description (tags broken — see bugs)
- `create_story` — with feature_id, title, description, priority (tags + estimated_minutes broken — see bugs)
- `update_story` — title, description, priority, acceptance_criteria

**Workflow**
- `start_story` — backlog → in_progress, assigns agent
- `move_story` — arbitrary status transitions with comment
- `request_review` — in_progress → review
- `escalate_story` — returns story to backlog, auto-creates arch-review story ✅

**Utilities**
- `add_comment` — on story, feature, or epic; visible in event history
- `create_tdd_cycle` — creates RED/GREEN/REFACTOR sub-stories under parent ✅
- `link_stories` — blocks / duplicates / relates_to directional links
- `get_story_links` — returns all links for a story
- `link_worktree` — links git branch to a story

**Note:** `complete_story` is broken (see bugs). Workaround: use `move_story` with status `done`.

---

## 🐛 Bugs

### BUG-1 (HIGH) — `complete_story`: boolean param rejected as string · BOARD-19
`checklist_confirmed: true` is passed as the string `"true"` by the MCP tool interface. Zod `z.boolean()` rejects it.

**Fix:** Use `z.preprocess(v => v === 'true' || v === true, z.boolean())` in the Zod schema, or use `z.coerce.boolean()`.

---

### BUG-2 (HIGH) — `create_feature` / `create_story`: array and number params rejected · BOARD-20
- `tags: ["auth"]` → `Expected array, received string`
- `estimated_minutes: 5` → `Expected number, received string`

**Root cause:** MCP SDK serializes all tool parameters as strings before passing them to the handler. Affects any Zod schema using `z.array()` or `z.number()` directly.

**Fix:** Add `z.preprocess` coercion for affected fields:
```ts
tags: z.preprocess(
  v => typeof v === 'string' ? JSON.parse(v) : v,
  z.array(z.string()).optional()
)
estimated_minutes: z.coerce.number().optional()
```

---

### BUG-3 (MEDIUM) — `delete_story_link`: crashes with "Unexpected end of JSON input" · BOARD-21
The DELETE endpoint returns HTTP 204 with no body. `board.ts` calls `res.json()` unconditionally, crashing when the body is empty.

**Fix:** In `board.ts`, guard JSON parsing:
```ts
const text = await res.text()
return text ? JSON.parse(text) : null
```

---

### BUG-4 (MEDIUM) — Write operations reject short_ids · BOARD-22
`start_story('BOARD-1', ...)` returns 404. All write endpoints require UUIDs. Read endpoints (`get_epic`, `get_story`) already accept short_ids — write endpoints should too.

**Fix:** In each write route, resolve short_id → UUID before operating:
```ts
const row = db.prepare('SELECT id FROM stories WHERE id = ? OR short_id = ?').get(id, id)
```

---

## 💡 Missing Features / Enhancements

### MISSING-1 (MEDIUM) — `update_feature` tool · BOARD-23
No way to edit a feature's title, description, or tags after creation. `update_story` and `update_epic` exist — feature is the gap.

---

### MISSING-2 (MEDIUM) — Delete tools for story / feature / epic · BOARD-24
No `delete_story`, `delete_feature`, or `delete_epic`. Agents cannot remove erroneous entities. Critical for iterative workflows and test cleanup.

---

### MISSING-3 (MEDIUM) — `list_stories` with filters · BOARD-25
Stories are only accessible via `get_board` (all) or `get_feature` (by feature). No way to query:
- Stories in a specific status across a project
- Stories assigned to a specific agent
- Stories matching a tag

Needed for agent dashboards ("what's my work queue?").

---

### MISSING-4 (LOW) — Doc watcher requires filesystem access · BOARD-26
The doc watcher monitors `DOCS_PATH` on the server. Against a deployed Railway instance, agents cannot write files. An HTTP endpoint (`POST /api/docs/sync` with markdown body) would make doc-driven board population work remotely.

---

### MISSING-5 (LOW) — No way to create public projects via MCP · BOARD-27
`create_project` always creates private projects (`is_public=0`). No MCP tool exposes the `is_public` flag or a `make_project_public` operation.

---

## Board State After Audit

- **Project:** BOARD (Agent Board)
- **Epics:** 5 (E1–E5)
- **Features:** 15 (F1–F15)
- **Stories:** 27 (BOARD-1 through BOARD-27)
- **BOARD-1** (req.user auth fix): `done` ✅ — fixed and deployed during this session
- **BOARD-10** (happy path workflow): `done` ✅ — confirmed via audit
- **BOARD-19 to BOARD-22**: bugs filed under E5/F14
- **BOARD-23 to BOARD-27**: enhancements filed under E5/F15

---

## Recommended Priority Order

1. **BUG-2** — Fix array/number Zod coercion (unblocks tags + estimated_minutes everywhere)
2. **BUG-1** — Fix boolean coercion (unblocks `complete_story`)
3. **BUG-3** — Fix `delete_story_link` empty body crash
4. **BUG-4** — Short_id support on write endpoints (major DX improvement)
5. **MISSING-1** — `update_feature` tool
6. **MISSING-2** — Delete tools
7. **MISSING-3** — `list_stories` with filters
