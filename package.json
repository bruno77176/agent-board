{
  "name": "agent-board",
  "private": true,
  "workspaces": ["server", "client", "mcp"],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=server && npm run build --workspace=mcp",
    "build:local": "npm run build --workspace=client && npm run build --workspace=server && npm run build --workspace=mcp",
    "start": "npm run start --workspace=server"
  }
}
